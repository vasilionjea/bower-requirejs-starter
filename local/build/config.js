({
	appDir: '../../local', // App's root directory
	baseUrl: 'js', // Base url for importing/requiring scripts (* relative to appDir)
	dir: '../../remote', // Where to build to
	mainConfigFile: '../js/main.js', // The app's main config file
	name: 'main', // I guess it needs a name too
	optimizeCss: 'standard' // Remove comments, spaces, line breaks, etc.
})